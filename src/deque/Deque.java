package deque;

import queue.Queue;
import stack.Stack;

public interface Deque<T> extends Stack<T>, Queue<T> {
    // TODO: create methods with better names that map to the Stack and Queue methods (like java.util does)
}
