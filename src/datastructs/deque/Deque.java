package datastructs.deque;

import datastructs.queue.Queue;
import datastructs.stack.Stack;

public interface Deque<T> extends Stack<T>, Queue<T> {
    // TODO: create methods with better names that datastructs.map to the Stack and Queue methods (like java.util does)
}
